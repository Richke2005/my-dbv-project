import React, { useState } from "react";
import { SafeAreaView, View, Text } from "react-native";

export default function SocialClubScreen({route, navigation}){
    return <SafeAreaView>
        <View>
            <Text>Clubs</Text>
        </View>
    </SafeAreaView>;
}